<!--
  PREVIEW ONLY — © Laura Riera
  Generated from private repository. Not licensed for redistribution.
  Full implementation: private. Contact for demo.
-->

> **Portfolio preview** — excerpt only. Remaining content is redacted.

---
name: worktree
description: Create a git worktree in a sibling directory with a new feature branch, ready to work on an isolated task. Triggers like "crea un worktree", "nova branca aïllada", "worktree", "start isolated task".
argument-hint: "[task-name in kebab-case, e.g. order-migration]"
allowed-tools: Bash
---

# Worktree: Create an isolated workspace

Create a git worktree in a sibling directory with a dedicated feature branch, based on the vault's branch naming convention.

## Parameters

- `$ARGUMENTS`: Required. Task name in kebab-case (e.g. `order-migration`, `fix-login-bug`).

If empty, ask the user for a task name before proceeding.

## Step 1 — Validate
---

<!-- REDACTED: remainder omitted -->

[Content redacted — full version available in private repository]

*This preview shows structure and approach, not operational instructions.*
